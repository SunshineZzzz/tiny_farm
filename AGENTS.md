# Repository Guidelines

## 项目结构与模块组织
这是一个精简版 Go 游戏客户端骨架，当前结构以单主循环为中心：

- `main.go`：程序根入口。
- `game`：游戏层装配代码，例如 `Run()` 和初始场景注册。
- `engine/core`：主循环、帧率控制、应用生命周期。
- `engine/context`：运行时共享上下文类型。

新增代码时，通用框架能力放到 `engine/...`，具体游戏逻辑放到 `game/...`。当前没有独立资源目录和测试目录，测试文件与代码放在同目录。

## 构建、测试与开发命令
- `go run .`：运行当前客户端入口。
- `go build .`：构建程序并检查编译错误。
- `go test ./...`：执行全部包测试。
- `go fmt ./...`：统一 Go 代码格式。

所有命令都从仓库根目录执行，例如 `D:\github\tiny_farm`。

## 代码风格与命名约定
遵循 Go 标准风格，保持代码可被 `gofmt` 直接格式化。包名小写简短；导出标识符用 PascalCase，非导出标识符用 camelCase。

注释风格按 `engine/core/fps.go` 保持统一：
- 结构体前先写一段总述，说明职责和当前方案边界。
- 关键字段用短句说明含义、单位或用途。
- 关键函数先写“做什么”，再写“当前为什么这样实现”。
- 注释统一用简洁中文，避免乱码和无信息量注释。
- 代码注释不要以英文句号 `.` 或中文句号 `。` 结尾。
- 结构体、函数、方法的注释不要重复写一遍结构体名称或函数名称，直接说明职责或行为。

## 帧率方案说明
当前版本明确使用“相对帧率”方案，不是绝对时间对齐方案。也就是每帧根据上一帧到当前帧的耗时决定是否补等待时间。提交这一路线的变更时，应在说明里明确写出“当前使用的是相对帧率方案”，避免和后续可能引入的绝对对齐实现混淆。

## 测试约定
测试使用 Go 标准库 `testing`。测试文件命名为 `*_test.go`，放在被测代码同目录。修改主循环、控帧逻辑、启动链路时，应优先补对应测试。

## 提交与合并请求规范
当前历史里同时存在前缀式提交和中文概述式提交。后续建议保持一句话说明一个改动，简短、直接、可读。

提交 PR 或整理提交说明时，至少包含：
- 本次改动解决了什么问题。
- 当前帧率方案是“相对帧率”还是其他方案。
- 实际执行过的命令，例如 `go test ./...`。
